# Scripts to run whenever Sway is reloaded

exec_always eww_update_vars "$theme" $colorscheme

exec_always (eww close bar0 bar1 calendar0 calendar1 > /dev/null 2>&1 &) && (killall waycorner > /dev/null 2>&1 &)
